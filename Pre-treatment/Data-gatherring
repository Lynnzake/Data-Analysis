#数据集成
        #数据集成一般是把不同来源的数据放在一起。但是来自多个地方的数据一定要做好实体识别与冗余属性识别
        #避免数据整合错误及数据重复
        一般来说，数据集成的过程：
        1.观察数据源，发现其中关系，详细查看是否有同名不同意，同意不同名的情况（如两个不同数据库里的title是否指的都是商品的
        店名还是商品名）；
        2.进行数据读取与整合；
        3.去除重复数据
        import numpy as np 
        a=np.array([[2,4,7],[45,78,90]])
        b=np.array([[21,44,75],[4,8,9]])
        c=np.concatenate((a,b))
